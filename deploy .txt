1- Create gitignore file
	.gitignore

2- create requirements.txt file

3-python -m venv ./venv
4-.\venv\Scripts\activate
5-pip install scikit-learn flask    -> install all the librarys that you used in the project

-> install gunicorn and the version in the requirements file.   pip install gunicorn
6-python app.py
7- pip freeze > requirements.txt

* Create a repo on github and push your files.












flask file -> app.py

from flask import Flask
import numpy as np
import pickle

app = Flask(__name__)
model = pickle.load(open('best_rf_model.pkl', 'rb'))

@app.route('/')
def home():
    return "Hello from Second model"

if __name__ == '__main__':
    app.run(host = '0.0.0.0',port=5001)


